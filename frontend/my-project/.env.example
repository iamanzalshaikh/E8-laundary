# API base URL (used when you run npm run build / production).
# For local dev, leave unset to use http://localhost:5000/api/v1
# For Render: set this in Render Dashboard → Frontend service → Environment
VITE_API_URL=https://e8-laundary-backend.onrender.com/api/v1
